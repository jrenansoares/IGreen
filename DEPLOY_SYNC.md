# iGreen Deploy Sync

Este arquivo registra a versão que contém:

- imagens WebP otimizadas;
- code splitting;
- otimizações de LCP;
- lazy loading;
- dimensões explícitas das imagens;
- preload metadata dos vídeos;
- regras de cache do Cloudflare.
